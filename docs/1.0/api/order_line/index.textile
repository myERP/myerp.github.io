---
layout: default
title: "- REST API V1.0 Resources - Order Line"
---

h1(blue newsection). General

p. In myERP, order lines are used in several places to register an order of a product or service, where information like price, quantity, etc... are requiered. So, in the API, order lines are embeded into their own JSON format, and follow the same rules everywhere.

h1(blue newsection). Context

p. Order lines don't exist on their own. They exist within a context. They are _inner objects_ embeded into other data and in order to access them you have to declare the context within the URI, like _invoices/:invoice_id/order_line/:id_. 

h3. Available context

|_. Context URI |_. Description |_. Context Type |_. In context URI sample |
| /sales_orders/:sales_order_id | Order lines for Sales order | "Sales Order":/docs/1.0/api/sales_order/#format | /sales_orders/:sales_order_id/order_line/:id |


h1(blue newsection#format). Order Line Format

|_. {{ site.format.header.field_name }} |_. {{ site.format.header.description }} |_. {{ site.format.header.type }} |_. {{ site.format.header.values }} |_. {{ site.format.header.default }} |_. {{ site.format.header.non_editable }} |
| id | Code | {{ site.format.type.long }} |  | Generated by the system |  |=. {{ site.format.non_editable }} |
| item | Product or Service | {{ site.format.type.long }} | IDs from Services or Products | {{ site.format.mandatory }} |  |
| description | Description | {{ site.format.type.text }} |  | The Product or Service default description |  |
| quantity | Quantity | {{ site.format.type.double }} |  | {{ site.format.mandatory }} |  |
| unit_price_wt | Unit Price Without Tax | {{ site.format.type.double }} |  | The Product or Service default sales price |  |
| discount_rate | Line Discount Rate | {{ site.format.type.double }} |  | 0.00 |  |
| tax | Tax | {{ site.format.type.long }} |  | None |  |
| is_taxable | Shall the tax be applied ? (US only) | {{ site.format.type.boolean }} |  | false |  |
| total | Total before taxes and discount | {{ site.format.type.double }} | {{ site.format.read_only }} | {{ site.format.non_available }} |=. {{ site.format.non_editable }} |

h1(blue newsection#order_line). order_lines/:id

h2(#order_line_get). GET ../order_line/:id

p. Returns all the information about the sales order, including its order lines.

h3. Resource Information

* Rate limited: No
* Authentication: Required
* HTTP Methods: GET
* Response Format: JSON

h3. Example Request

{% highlight bash %}
curl -X GET {{ site.api.base_url }}{{ site.api.v1}}/../{{ site.resource.order_lines.url}}/:id {{site.api.auth_pattern}}
{% endhighlight %}

p. JSON response sample

{% highlight bash %}
{
  "id": 847,
  "item": 463,
  "description": "Nintendo WiiU\nBlack",
  "quantity": 5.0,
  "unit_price_wt": 299.99,
  "discount_rate": 0.0,
  "isTaxable": false,
  "total": 1499.95
}
{% endhighlight %}


h2(#order_line_put). PUT ../order_lines/:id

p. Update a single sales order. Use /order_lines/:id to edit order lines.

h3. Resource Information

* Rate limited: No
* Authentication: Required
* HTTP Methods: PUT
* Body Format: JSON
* Response Format: JSON
* Non editable fields: id, user_code

h3. Example Request

{% highlight bash %}
curl -X PUT {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.order_lines.url}}/:id {{site.api.auth_pattern}} -H 'Content-Type: application/json' -T fieldsToUpdate.json
{% endhighlight %}

p. JSON response

The up-to-date JSON of the sales order you've just modfied.


h2(#order_line_delete). DELETE ../order_lines/:id

p. Deletes a single sales order and all its order lines.

h3. Resource Information

* Rate limited: No
* Authentication: Required
* HTTP Methods: DELETE
* Response Format: JSON

h3. Example Request

{% highlight bash %}
curl -X DELETE {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.sales_orders.url}}/:id {{site.api.auth_pattern}}
{% endhighlight %}

p. JSON response

The JSON of the sales order you've just deleted.


h1(blue newsection#order_lines). order_lines

h2(#order_lines_get).  GET ../order_lines

p. Returns the list of all your sales orders.

h3. Resource Information

* Rate limited: No
* Authentication: Required
* HTTP Methods: GET
* Body Format: JSON
* Response Format: JSON

h3. Example Request

{% highlight bash %}
curl -X GET {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.order_lines.url}} {{site.api.auth_pattern}}
{% endhighlight %}

p. JSON response

The list of all your sales orders, in a JSON format.


h2(#order_lines_post). POST ../order_lines

p. Create a single sales order and its order lines.

h3. Resource Information

* Rate limited: No
* Authentication: Required
* HTTP Methods: POST
* Body Format: JSON
* Response Format: JSON
* Mandatory fields : customer

h3. Example Request

{% highlight bash %}
curl -X POST {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.order_lines.url}} {{site.api.auth_pattern}} -H 'Content-Type: application/json' -T newAccount.json
{% endhighlight %}

p. JSON response

The complete JSON of the sales order, as it was created by the system.


h2(#order_lines_put). PUT ../order_lines

p. Create and Update several sales orders in a row.

h3. Resource Information

* Rate limited: No
* Authentication: Required
* HTTP Methods: PUT
* Body Format: JSON
* Response Format: JSON
* Mandatory fields for creation: customer
* Non editable fields for edition: id, user_code

h3. Example Request

{% highlight bash %}
curl -X PUT {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.order_lines.url}} {{site.api.auth_pattern}} -H 'Content-Type: application/json' -T listOfAccounts.json
{% endhighlight %}

p. JSON response

A JSON list made of up-to-date info of sales orders successfully edited or created, and error messages for unsuccessful ones.


h2(#order_lines_delete).  DELETE ../order_lines

p. Delete several sales orders and their order lines in a row.

* Rate limited: No
* Authentication: Required
* HTTP Methods: DELETE
* Body Format: JSON
* Response Format: JSON

h3. Example Request

{% highlight bash %}
curl -X DELETE {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.order_lines.url}} {{site.api.auth_pattern}} -H 'Content-Type: application/json' -T listOfIDsToDelete.json
{% endhighlight %}

p. JSON response

A JSON list made of the complete info of sales orders successfully deleted, and error messages for unsuccessful ones.
