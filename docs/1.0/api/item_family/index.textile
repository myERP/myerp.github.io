---
layout: default
title: "- REST API V1.0 Resources - Item Family"
---

span(anchor#format). ~

h1(blue). Item Family Format

|_. {{ site.format.header.field_name }} |_. {{ site.format.header.description }} |_. {{ site.format.header.type }} |_. {{ site.format.header.values }} |_. {{ site.format.header.default }} |_. {{ site.format.header.non_editable }} |
| id | Code | {{ site.format.type.long }} |  | Generated by the system |=. {{ site.format.non_editable }} |
| name | Name | {{ site.format.type.string64 }} |  | {{ site.format.mandatory }} |  |
| code | Short Code | {{ site.format.type.string64 }} |  | Generated by the system |=. {{ site.format.non_editable }} |


span(anchor#item_family_get). ~

h2. GET item_families/:id

p. Returns the code and the name of the item family.

h3. Resource Information

* Rate limited: Yes
* Authentication: Required
* HTTP Methods: GET
* Response Format: JSON

h3. Example Request

{% highlight bash %}
curl -X GET {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.item_families.url}}/:id {{site.api.auth_pattern}}
{% endhighlight %}

p. JSON response sample

{% highlight bash %}
{
  "id": 4,
  "name": "Video Game",
  "code": "FAM-007"
}
{% endhighlight %}



span(anchor#item_family_put). ~

h2. PUT item_families/:id

p. Update a single item family.

h3. Resource Information

* Rate limited: Yes
* Authentication: Required
* HTTP Methods: PUT
* Body Format: JSON
* Response Format: JSON
* Non editable fields: id, code

h3. Example Request

{% highlight bash %}
curl -X PUT {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.item_families.url}}/:id {{site.api.auth_pattern}} -H 'Content-Type: application/json' -T fieldsToUpdate.json
{% endhighlight %}

p. JSON response

The up-to-date JSON of the item family you've just modfied.


span(anchor#item_family_delete). ~

h2. DELETE item_families/:id

p. Deletes a single item family.

h3. Resource Information

* Rate limited: Yes
* Authentication: Required
* HTTP Methods: DELETE
* Response Format: JSON

h3. Example Request

{% highlight bash %}
curl -X DELETE {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.item_families.url}}/:id {{site.api.auth_pattern}}
{% endhighlight %}

p. JSON response

The JSON of the item family you've just deleted.


span(anchor#item_families_get). ~

h2.  GET item_families

p. Returns the list of all your item families.

h3. Resource Information

* Rate limited: Yes
* Authentication: Required
* HTTP Methods: GET
* Body Format: JSON
* Response Format: JSON

h3. Example Request

{% highlight bash %}
curl -X GET {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.item_families.url}} {{site.api.auth_pattern}}
{% endhighlight %}

p. JSON response

The list of all your item families, in a JSON format.


span(anchor#item_families_post). ~

h2. POST item_families

p. Create a single item family.

h3. Resource Information

* Rate limited: Yes
* Authentication: Required
* HTTP Methods: POST
* Body Format: JSON
* Response Format: JSON
* Mandatory fields : name

h3. Example Request

{% highlight bash %}
curl -X POST {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.item_families.url}} {{site.api.auth_pattern}} -H 'Content-Type: application/json' -T newItemFamily.json
{% endhighlight %}

p. JSON response

The complete JSON of the item family, as it was created by the system.


span(anchor#item_families_put). ~

h2. PUT item_families

p. Create and Update several item families in a row.

h3. Resource Information

* Rate limited: Yes
* Batch limit: {{ site.api.batch_limit }}
* Authentication: Required
* HTTP Methods: PUT
* Body Format: JSON
* Response Format: JSON
* Mandatory fields for creation: name
* Non editable fields for edition: id, code

h3. Example Request

{% highlight bash %}
curl -X PUT {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.item_families.url}} {{site.api.auth_pattern}} -H 'Content-Type: application/json' -T listOfItemFamilies.json
{% endhighlight %}

p. JSON response

A JSON list made of up-to-date info of item families successfully edited or created, and error messages for unsuccessful ones.


span(anchor#item_families_delete). ~

h2.  DELETE item_families

p. Delete several item families in a row

* Rate limited: Yes
* Batch limit: {{ site.api.batch_limit }}
* Authentication: Required
* HTTP Methods: DELETE
* Body Format: JSON
* Response Format: JSON

h3. Example Request

{% highlight bash %}
curl -X DELETE {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.item_families.url}} {{site.api.auth_pattern}} -H 'Content-Type: application/json' -T listOfIDsToDelete.json
{% endhighlight %}

p. JSON response

A JSON list made of the complete info of item families successfully deleted, and error messages for unsuccessful ones.
