---
layout: default
title: "- REST API V1.0 Resources - Item Family"
---

h1(blue newsection#format). Item Family Format

|_. {{ site.format.header.field_name }} |_. {{ site.format.header.description }} |_. {{ site.format.header.type }} |
| id | Code | {{ site.format.type.long }} |
| name | Name | {{ site.format.type.string64 }} |


h1(blue newsection#item_family). item_families/:id

h2(#item_family_get). GET item_families/:id

p. Returns the code and the name of the item family.

h3. Resource Information

* Rate limited: No
* Authentication: Required
* HTTP Methods: GET
* Response Format: JSON

h3. Example Request

{% highlight bash %}
curl -X GET {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.item_families.url}}/:id {{site.api.auth_pattern}}
{% endhighlight %}

p. JSON response sample

{% highlight bash %}
{
  "id": 4,
  "name": "Video Game"
}
{% endhighlight %}


h1(blue newsection#item_families). item_families

h2(#item_families_get).  GET item_families

p. Returns the list of all your item families.

h3. Resource Information

* Rate limited: No
* Authentication: Required
* HTTP Methods: GET
* Body Format: JSON
* Response Format: JSON

h3. Example Request

{% highlight bash %}
curl -X GET {{ site.api.base_url }}{{ site.api.v1}}{{ site.resource.item_families.url}} {{site.api.auth_pattern}}
{% endhighlight %}

p. JSON response

The list of all your item families, in a JSON format.
